排列算法
- 新增空闲区域检测和自动填充。
- 宽立牌占两列时，右侧第三列可独立补位。
- 支持跨页寻找可放入的图片组。
- 表头按横向、纵向范围准确归属。
- 删除或重新排列后清理无效表头和空区域。

立牌、照片夹和串串布局
- 识别“照片夹”等中文产品名称。
- 立牌 Front/Back 布局和宽度逻辑调整。
- 保持图片等比显示，不随意放大或压缩。
- 修正底座、背面图和不同图正反面的尺寸。

尺寸、标尺和属性区
- 单图在图片区域内垂直居中。
- 标尺改为贴合图片真实左右边缘。
- 旧数据中的属性区位置会自动限制在图片组范围内。
- 备注文字和备注图片居中。
- 配件白底、裁剪和缩放规则调整。

远程配件图片
- 新增 Rust fetch_remote_image 命令。
- Tauri 中转远程图片为 data URL，解决跨域。
- Vite 开发环境增加远程图片代理。
- 远程图片限制为图片类型，最大 12MB。
- 增加远程图片缓存。

上传批次
- 首次上传 SVG 时自动使用文件名填充客户名称。
- 上传失败时清理临时批次名称和状态。

界面文字
- “编辑产品组”改成“编辑组合”。
- “开始图片组合引导”改成“图片组合”。

测试和文档
- 增加空闲区域排列测试。
- 增加照片夹、立牌、标尺、属性区、备注和配件图片测试。
- 更新排列规则和分组说明文档。